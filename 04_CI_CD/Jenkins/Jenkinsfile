pipeline {
    agent any

    environment {
        PYTHON = 'C:\\Users\\kinga\\AppData\\Local\\Programs\\Python\\Python312\\python.exe'
    }

    stages {

        stage('Checkout') {
            steps {
                echo '=========================================='
                echo 'Checkout Source Code'
                echo '=========================================='

                checkout scm
            }
        }

        stage('Environment Check') {
            steps {
                echo '=========================================='
                echo 'Environment Check'
                echo '=========================================='

                bat "\"%PYTHON%\" --version"
                bat "\"%PYTHON%\" -m pip --version"
            }
        }

        stage('Install Dependencies') {
            steps {
                echo '=========================================='
                echo 'Install Dependencies'
                echo '=========================================='

                bat "\"%PYTHON%\" -m pip install -r requirements.txt"
            }
        }

        stage('Install Playwright Browser') {
            steps {
                echo '=========================================='
                echo 'Install Playwright Browser'
                echo '=========================================='

                bat "\"%PYTHON%\" -m playwright install chromium"
            }
        }

        stage('Start Flask Servers') {
            steps {
                echo '=========================================='
                echo 'Start Flask Servers'
                echo '=========================================='

                echo 'Starting UI Flask Server : 5000'

                bat "start \"UI Flask Server\" /B \"%PYTHON%\" 03_Mock_Server\\app.py"

                echo 'Starting API Flask Server : 5001'

                bat "start \"API Flask Server\" /B \"%PYTHON%\" 02_API_Automation\\mock_server\\robot_api.py"

                echo 'Both Flask Servers started'

                sleep time: 3, unit: 'SECONDS'
            }
        }

        stage('API Test') {
            steps {
                echo '=========================================='
                echo 'API Test'
                echo '=========================================='

                bat "\"%PYTHON%\" -m pytest 02_API_Automation\\tests\\ -s"
            }
        }

        stage('Playwright Test') {
            steps {
                echo '=========================================='
                echo 'Playwright UI Test'
                echo '=========================================='

                bat "\"%PYTHON%\" -m pytest 01_UI_Automation\\Playwright\\tests\\ -s"
            }
        }
    }

    post {

        always {
            echo '=========================================='
            echo 'Jenkins Pipeline Finished'
            echo '=========================================='
        }

        success {
            echo 'TEST RESULT : PASS'
        }

        failure {
            echo 'TEST RESULT : FAIL'
        }
    }
}

